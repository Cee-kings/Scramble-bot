# fly.toml app configuration file generated for scramble-bot on 2026-05-24T06:19:07Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'scramble-bot'
primary_region = 'ams'

[build]
  dockerfile = 'Dockerfile'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

[[vm]]
  memory = '100mb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 100
